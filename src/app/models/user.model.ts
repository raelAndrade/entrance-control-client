export class User {
  id: number;
  name: string;
  email: string;
  password: string;
  profile: string;

  constructor(
    id: number,
    name: string,
    email: string,
    password: string,
    profile: string
  ) {
  }
}
